import { Prata, DM_Sans, Lato } from "next/font/google";

export const title = Prata({
	weight: "400",
	subsets: ["latin"],
});

export const subTitle = Lato({
	weight: "400",
	subsets: ["latin"],
});

export const text = DM_Sans({
	weight: "400",
	subsets: ["latin"],
});
